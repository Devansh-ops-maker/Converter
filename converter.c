#include <stdio.h>
#include <math.h>

int main()
{
int n;

printf("Enter a number in decimal format");

scanf("%d",&n);

printf("The given number in octal format is %o: \n",n);

printf("The given number in hexadecimal format is: %x \n",n);

}
